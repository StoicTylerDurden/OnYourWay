<nav>
    <% if (user) { %>
  
    <a href="/">Home</a>

    <a href="/users/<%= user._id %>/trips/available">Available</a>
    <a href="/users/<%= user._id %>/trips">Your Created Trips</a>
    <a href="/users/<%= user._id %>/trips/booked">Your Booked Trips</a>

    <a href="/auth/sign-out">Sign Out</a>

    <% } else { %>
  
    <a href="/auth/sign-in">Sign In</a>
    <a href="/auth/sign-up">Sign Up</a>
  
    <% } %>
  </nav>