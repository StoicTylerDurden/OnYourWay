<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Booked Trips</title>
</head>
<body>
  <%- include('../partials/_navbar.ejs') %>

  <h1>Your Booked Trips</h1>
  <ul>
    <% bookedTrips.forEach((trip) => { %>
      <li>
        <a href="/users/<%= trip.userId %>/trips/<%= trip._id %>">
          <%= trip.startLocation %> to <%= trip.endLocation %>
        </a>
        <form action="/users/<%= user._id %>/trips/booked/<%= trip._id %>?_method=DELETE" method="POST" style="display:inline;">
          <button type="submit">Delete</button>
        </form>
      </li>
    <% }) %>
  </ul>
</body>
</html>
